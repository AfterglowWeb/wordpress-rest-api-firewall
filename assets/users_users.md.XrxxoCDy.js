import{_ as t,o as s,c as r,ag as a}from"./chunks/framework.BPmc_f4o.js";const c=JSON.parse('{"title":"Auth & Rate Limiting","description":"","frontmatter":{},"headers":[],"relativePath":"users/users.md","filePath":"users/users.md"}'),o={name:"users/users.md"};function i(n,e,l,d,p,u){return s(),r("div",null,[...e[0]||(e[0]=[a('<p><span style="display:inline-block;padding:1px 8px;border-radius:3px;background:#2e7d32;color:#fff;font-size:11px;font-weight:600;">FREE</span> <span style="display:inline-block;padding:1px 8px;border-radius:3px;background:#1565c0;color:#fff;font-size:11px;font-weight:600;">PRO</span></p><h1 id="auth-rate-limiting" tabindex="-1">Auth &amp; Rate Limiting <a class="header-anchor" href="#auth-rate-limiting" aria-label="Permalink to &quot;Auth &amp; Rate Limiting&quot;">​</a></h1><p>The Auth &amp; Rate Limiting panel manages which WordPress users have access to the REST API, which authentication methods are accepted, and what rate quotas apply.</p><p>In the <strong>free tier</strong>, users and rate limits are configured globally and apply across all routes and requests.</p><p>In <strong>Pro</strong>, each <a href="/applications/applications.html">Application</a> defines its own auth methods, allowed origins, allowed IPs, allowed HTTP methods, and user list. Users belong to an application and per-user settings can narrow — but never relax — the application-level defaults.</p><hr><h2 id="free-tier" tabindex="-1">Free Tier <a class="header-anchor" href="#free-tier" aria-label="Permalink to &quot;Free Tier&quot;">​</a></h2><h3 id="global-settings" tabindex="-1">Global Settings <a class="header-anchor" href="#global-settings" aria-label="Permalink to &quot;Global Settings&quot;">​</a></h3><p>Authentication and rate limiting enforcement is toggled in the <a href="/routes/routes.html">Routes panel</a>:</p><ul><li><strong>Enforce Authentication</strong> — requires a valid authenticated request on all WordPress core REST endpoints.</li><li><strong>Enforce Rate Limiting</strong> — applies the global quota to all routes.</li></ul><h3 id="user-entries" tabindex="-1">User Entries <a class="header-anchor" href="#user-entries" aria-label="Permalink to &quot;User Entries&quot;">​</a></h3><p>Each entry links a WordPress user to an optional rate limit quota. When the module is enabled, authenticated requests are checked against this list before the endpoint is reached.</p><details><summary>User Identity</summary><p><strong>User</strong> links this entry to an existing WordPress user account.</p><p><strong>Enabled</strong> toggles activation for this user without deleting the entry.</p></details><details><summary>Rate Limiting</summary><p><strong>Window (seconds)</strong> defines the time period over which requests are counted. For example, <code>60</code> means one minute.</p><p><strong>Max Requests</strong> is the maximum number of REST API requests allowed within one window. When exceeded, the firewall returns <code>429 Too Many Requests</code> until the window resets. Leave both fields empty to grant unrestricted access while still tracking the user.</p><p><strong>Block Time</strong> is how long an IP is blocked after exceeding the max requests threshold.</p><p><strong>Blacklist Threshold</strong> is the number of times the max requests limit must be hit before the IP is automatically blacklisted.</p></details><hr><h2 id="pro-—-application-level-defaults" tabindex="-1">Pro — Application-level Defaults <a class="header-anchor" href="#pro-—-application-level-defaults" aria-label="Permalink to &quot;Pro — Application-level Defaults&quot;">​</a></h2><p>When the Auth &amp; Rate Limiting module is active for an application, the following defaults apply to every user in that application:</p><ul><li><strong>Allowed auth methods</strong> — accepted authentication mechanisms (WordPress Application Password, JWT). Requests using a non-listed method are rejected with <code>403</code>.</li><li><strong>Allowed HTTP methods</strong> — permitted verbs across all routes for this application (<code>GET</code>, <code>POST</code>, <code>PUT</code>, <code>PATCH</code>, <code>DELETE</code>).</li><li><strong>Allowed IPs</strong> — optional IP allowlist. Requests from unlisted IPs are rejected.</li><li><strong>Allowed origins</strong> — optional origin allowlist. Requests from unlisted origins are rejected.</li><li><strong>Default rate limit</strong> — maximum requests and time window applied to all users of this application unless overridden at user level.</li></ul><p>User-level settings can only be <strong>more restrictive</strong>, never more permissive.</p><hr><h2 id="pro-—-users-list" tabindex="-1">Pro — Users List <a class="header-anchor" href="#pro-—-users-list" aria-label="Permalink to &quot;Pro — Users List&quot;">​</a></h2><p>The users list manages which WordPress users have access to the application.</p><ul><li><strong>Add</strong> a user with the add button.</li><li><strong>Enable / Disable</strong> a user entry. A confirmation is required.</li><li><strong>Delete</strong> one or more users. A confirmation is required.</li></ul><p>An application supports an <strong>unlimited number of users</strong>.</p><hr><h2 id="pro-—-user-editor" tabindex="-1">Pro — User Editor <a class="header-anchor" href="#pro-—-user-editor" aria-label="Permalink to &quot;Pro — User Editor&quot;">​</a></h2><p>Accessible from the users list, the user editor lets you configure per-user access rules that narrow the application defaults:</p><ul><li><strong>User</strong> — select the WordPress user account this entry applies to.</li><li><strong>Auth method</strong> — if multiple methods are allowed at application level, restrict this user to a single one.</li><li><strong>Auth keys</strong> — for methods other than WordPress Application Password (which is self-contained), provide the relevant credentials or token.</li><li><strong>Allowed HTTP methods</strong> — restrict to a subset of the methods permitted at application level.</li><li><strong>Allowed IPs</strong> — narrow to one or more specific IPs.</li><li><strong>Allowed origins</strong> — narrow to one or more specific origins.</li><li><strong>Rate limit</strong> — override the application default with a lower quota. Rate limits can only be tightened at user level, not relaxed.</li></ul><hr><h2 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h2><p><strong>Are rate limits shared across applications?</strong></p><p>No. In Pro, each user entry belongs to a specific application and its counter is scoped to that application. Use separate user entries per application to keep quotas isolated.</p><p><strong>What happens to unauthenticated requests?</strong></p><p>Unauthenticated requests are not affected by user entries. To block them, enable authentication enforcement in the <a href="/routes/routes.html">Routes panel</a>.</p><p><strong>Can a user belong to multiple applications?</strong></p><p>Yes. Create a separate user entry in each application. The same WordPress user can have different rate limits, auth methods, and access rules per application.</p>',36)])])}const g=t(o,[["render",i]]);export{c as __pageData,g as default};
